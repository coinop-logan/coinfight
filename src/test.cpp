#include <iostream>
#include <optional>
#include <typeinfo>
#include "engine.h"
#include "config.h"
#include "myvectors.h"

using namespace std;

int main()
{
    // Target t(4);
    
    // vch testBuffer;
    // t.pack(&testBuffer);

    // auto iter = testBuffer.begin();

    // Target t2(&iter);

    // if (auto p = t2.castToPoint())
    //     cout << p->x << "," << p->y << endl;
    //     p.and_then()
    // else if (auto e = t2.castToEntityRef())
    //     cout << *e << endl;
    // else {
    //     cout << "??" << endl;
    // }

    return 0;
}